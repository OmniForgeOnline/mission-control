---
"@omniforge/mission-control": patch
---

Review fixes: workflow pool pins fail loudly like task pins, legacy stage overrides preserved in ambiguousLegacy, and stage model pool setup uses migrated agent overrides.
