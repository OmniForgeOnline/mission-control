node_modules/
dist/
.DS_Store
.env
*.log
# Local browser-tooling artifacts (Playwright MCP snapshots/screenshots).
.playwright-mcp/
# npm pack output.
*.tgz
# Local harness state — memory, runs, tasks, indexes (personal, not repo-backed)
data/
!src/ui/data/
!src/ui/data/**
